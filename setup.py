import setuptools

setuptools.setup(
    name='KoNTA',
    version='0.0.1',
    description='',
    author='',
    packages=setuptools.find_packages(),
    install_requires=[
    ],
    classifiers=[
    ]
)