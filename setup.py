import setuptools

setuptools.setup(
    name='KoNTA',
    version='',
    description='',
    author='',
    packages=setuptools.find_packages(),
    install_requires=[
    ],
    classifiers=[
    ]
)